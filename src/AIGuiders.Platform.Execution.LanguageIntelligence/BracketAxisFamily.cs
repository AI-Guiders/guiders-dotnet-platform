namespace AIGuiders.Platform.Execution.LanguageIntelligence;

/// <summary>Legacy wire family classifier for F/M/L bracket axes (Execution-only; deleted from Modeling).</summary>
public enum BracketAxisFamily
{
    None = 0,
    Csharp = 1,
    Xml = 2,
    Navigation = 3,
    Fsharp = 4,
    Json = 5,
}
